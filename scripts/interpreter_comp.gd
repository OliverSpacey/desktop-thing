extends Node
class_name InterpreterComponent

#This components reads all inputs and interprets the actions to take based on context from the current scene and layout
#Component chosen so it can be added/subtracted to/from scenes to mimic device locking, incorrect input mappings, etc. more easily
